{
  "name": "resume-builder",
  "version": "1.0.0",
  "description": "Resume Builder Application",
  "scripts": {
    "build": "npm run build --workspaces",
    "build:client": "cd client && npm run build",
    "build:server": "echo 'Server build not required for Vercel'",
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run server --workspace=server\""
  },
  "workspaces": [
    "client",
    "server"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}